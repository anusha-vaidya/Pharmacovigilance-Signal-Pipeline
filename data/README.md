# data

This folder stores live snapshots fetched from OpenFDA by the pipeline.
- Snapshots are timestamped CSV files named like: live_snapshot_YYYYMMDDTHHMMSSZ.csv
- Large or frequent snapshots should use Git LFS or be uploaded as Action artifacts instead of committing to main.
- This README is a placeholder so the folder is tracked in git.
